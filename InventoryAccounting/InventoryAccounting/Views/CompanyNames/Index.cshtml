@model IEnumerable<InventoryAccounting.Models.DB.CompanyName>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DirectorsName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DirectorsPhone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirectorsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirectorsPhone)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Unp">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Unp">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Unp">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
